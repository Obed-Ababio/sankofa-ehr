# frontend-esms

Custom O3 microfrontends, only where config cannot do it:
`esm-ghana-patient-search` (network search + consent UI, WP-022) and
`esm-ghana-claims` (Phase 4, WP-071). Follow O3 ESM conventions.
