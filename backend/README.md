# backend

Custom Maven OMODs. `ghanaemr-core/` (WP-020) holds the activator, config-flags
pattern, §8 adapter interfaces + mocks with contract tests, and REST resources —
see its README for the dockerized build. `ghanaemr-claims` is Phase 4 (WP-071).
JUnit >= 80% line coverage on service code, enforced by JaCoCo in the build.
