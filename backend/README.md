# backend

Custom Maven OMODs. `ghanaemr-core` scaffold lands with WP-020
(validators, consent/access service, adapter interfaces + mocks, REST resources).
`ghanaemr-claims` is Phase 4 (WP-071). JUnit >= 80% line coverage on service code.
