<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.ozonehis</groupId>
    <artifactId>maven-parent</artifactId>
    <version>1.0.0-alpha.10</version>
  </parent>

  <groupId>com.sankofa</groupId>
  <artifactId>sankofa-emr</artifactId>
  <version>1.0.0-SNAPSHOT</version>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>8</maven.compiler.source>
    <maven.compiler.target>8</maven.compiler.target>
    <!-- Initializer >= 2.7.0 is needed for the `queues` config domain (2.7);
         the refapp distro zip bundles 2.6.0, replaced during the build. -->
    <initializerVersion>2.12.0</initializerVersion>
    <!-- fhir2 < 2.5.0 does not enforce OpenMRS privileges (CVE-2025-46823,
         GHSA-g5vq-w8v2-4x9j) — any authenticated user could read clinical
         data via /ws/fhir2/R4. The refapp bundles 2.0.0; 2.5.1 is the
         patched line for platform 2.4.1+ (3.x/4.x need platform 2.7/2.8). -->
    <fhir2Version>2.5.1</fhir2Version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.openmrs.module</groupId>
      <artifactId>initializer-omod</artifactId>
      <version>${initializerVersion}</version>
      <type>jar</type>
    </dependency>
    <dependency>
      <groupId>org.openmrs.module</groupId>
      <artifactId>fhir2-omod</artifactId>
      <version>${fhir2Version}</version>
      <type>jar</type>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>Copy upgraded Initializer omod into the distro</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>org.openmrs.module</groupId>
                  <artifactId>initializer-omod</artifactId>
                  <version>${initializerVersion}</version>
                  <destFileName>initializer-${initializerVersion}.omod</destFileName>
                </artifactItem>
                <artifactItem>
                  <groupId>org.openmrs.module</groupId>
                  <artifactId>fhir2-omod</artifactId>
                  <version>${fhir2Version}</version>
                  <destFileName>fhir2-${fhir2Version}.omod</destFileName>
                </artifactItem>
              </artifactItems>
              <outputDirectory>
                ${project.build.directory}/${project.artifactId}-${project.version}/distro/binaries/openmrs/modules</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>Remove the refapp-bundled Initializer and fhir2 omods</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <delete>
                  <fileset
                    dir="${project.build.directory}/${project.artifactId}-${project.version}/distro/binaries/openmrs/modules"
                    includes="initializer-2.6.0.omod,fhir2-2.0.0.omod" />
                </delete>
                <!-- The refapp demo drug list (320 US-market drugs + 469
                     random-uuid drug concepts) would fight the NHIS formulary:
                     demo concept names collide with the CIEL FSNs we create,
                     and Initializer's intra-domain file order can't be relied
                     on to retire them before ours load (bit us in CI). The
                     Sankofa formulary fully replaces them, so remove the demo
                     files from the build instead. -->
                <delete>
                  <fileset
                    dir="${project.build.directory}/${project.artifactId}-${project.version}/distro/configs/openmrs/initializer_config"
                    includes="drugs/drugs-core_demo.csv,concepts/drugs_concepts-core_demo.csv" />
                </delete>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <!-- The parent copies only docker-compose-files.txt and
               start-ozone.sh from scripts/; our compose override file
               must land next to the other compose files in run/docker. -->
          <execution>
            <id>Copy docker-compose-override.yml</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>
                ${project.build.directory}/${project.artifactId}-${project.version}/run/docker</outputDirectory>
              <overwrite>true</overwrite>
              <resources>
                <resource>
                  <directory>${project.basedir}/scripts</directory>
                  <includes>
                    <include>docker-compose-override.yml</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>validator</id>
      <activation>
        <jdk>1.8</jdk>
        <property>
          <name>env.CI</name>
          <value>true</value>
        </property>
      </activation>
      <build>
        <plugins>
          <!-- hooking the OpenMRS config validation to the integration-test phase -->
          <plugin>
            <groupId>org.openmrs.maven.plugins</groupId>
            <artifactId>openmrs-packager-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>Validate OpenMRS configuration</id>
                <phase>integration-test</phase>
                <goals>
                  <goal>validate-configurations</goal>
                </goals>
                <configuration>
                  <sourceDir>${project.build.directory}/${project.artifactId}-${project.version}/distro/configs/openmrs/initializer_config</sourceDir>
                </configuration>
              </execution>
            </executions>
          </plugin>

        </plugins>
      </build>
    </profile>
  </profiles>
</project>
